# Import/Export + MIDI audit backlog (41 confirmed)

> **2.6.0 audit status (2026-06-19).** Cross-checked against the code by a
> two-pass automated audit (classify, then adversarial verify). An item is
> listed **RESOLVED** only when the adversarial pass confirmed the desired
> behavior is implemented; **PARTIAL** = implemented with caveats/gaps; the
> rest stay open. Overlay over 41 items — the per-item descriptions below
> are unchanged.

- **RESOLVED (9):**
  - **#1** — Crescendo/diminuendo wedges not imported
  - **#4** — MEI beam/tuplet container children dropped
  - **#5** — Only one MusicXML part imported; multi-staff collapses
  - **#8** — Lyrics (syllables) never exported
  - **#9** — Beams never exported
  - **#17** — Slur/tie <number> (id) ignored; overlapping slurs collapse
  - **#18** — Multi-voice measures export empty
  - **#19** — Ornaments and grace notes dropped on export
  - **#38** — MusicXML <duration> hardcoded to 1
- **PARTIAL (12):**
  - **#2** — Tuplets silently dropped on export
  - **#3** — MEI scoreDef/staffDef clef/key/meter lost
  - **#6** — MEI control events (slur/tie/dynam) with @startid/@endid ign...
  - **#10** — Clef line mapping wrong for treble/bass and missing octave c...
  - **#11** — Ties only emit start/stop, not continuation; <tied> notation...
  - **#14** — TempoMark.beatUnit ignored in playback
  - **#20** — Barlines and repeats not exported
  - **#24** — Crescendo/diminuendo produce no velocity ramp; reset velocit...
  - **#25** — Grace notes add time instead of stealing it
  - **#29** — Ornaments never expanded into note stream
  - **#33** — Grace-note slash (acciaccatura vs appoggiatura) not captured
  - **#37** — Only first <beam> level read; secondary beams lost

## 1. [HIGH/medium] (musicxml-import) Crescendo/diminuendo wedges (<wedge>) are not imported
**Current:** _parseMusicXmlDirections (parser_support.dart:1365) only handles direction-type children dynamics, words, metronome, segno, coda, rehearsal, and octave-shift. There is no case for <wedge>, so hairpin crescendo/diminuendo spanners are dropped entirely. The model supports them: Dynamic has isHairpin and length (lib/core/dynamic.dart:55-66) and DynamicType has crescendo/diminuendo (parser_support.dart:522-524), but only literal <dynamics> abbreviations and the words 'crescendo'/'diminuendo' ever reach it.
**Desired:** Map <wedge type="crescendo"|"diminuendo"> to Dynamic(type: crescendo/diminuendo, isHairpin: true) on start and close it on type="stop". Hairpins are extremely common in any expressive score.
**How:** Add case 'wedge' in _parseMusicXmlDirections: read getAttribute('type'); on 'crescendo'/'diminuendo' emit a hairpin Dynamic, on 'stop' optionally finalize length. Like octave-shift, this is a spanner so a start/stop pairing pass (or storing the open wedge on the parser) is needed to set length; minimally emit the start marker so the hairpin is not lost.

## 2. [HIGH/medium] (musicxml-export) Exporter silently drops all tuplets and every note inside them
**Current:** In `_buildMeasureXml` (lib/src/parsers/musicxml_parser.dart:209-237) the element loop handles Note, Rest, Chord, Dynamic, TempoMark and MusicText, but has NO `else if (element is Tuplet)` branch. The importer wraps triplet/tuplet content in a `Tuplet` element whose children live in `Tuplet.elements` (lib/core/tuplet.dart:35), so on export the Tuplet object is skipped entirely and all the notes it contains vanish from the output. `_buildNoteXml` also never emits `<time-modification>` or `<notations><tuplet type=...>`.
**Desired:** Iterate `Tuplet.elements`, emitting each child note/rest, and add `<time-modification><actual-notes>N</actual-notes><normal-notes>M</normal-notes></time-modification>` to every note plus `<notations><tuplet type="start"/></notations>` on the first and `type="stop"` on the last, using `Tuplet.actualNotes`/`normalNotes`.
**How:** Add a branch `else if (element is Tuplet) { for each child call _buildNoteXml/_buildRestXml }`. Pass actual/normal counts into `_buildNoteXml` so it can emit `<time-modification>` and the start/stop `<tuplet>` notation. This is the single biggest round-trip data-loss bug: import->export of any triplet produces an empty/short measure.

## 3. [HIGH/medium] (mei-import) <scoreDef>/<staffDef> are never read — clef, key, and meter are lost for virtually all real-world MEI files
**Current:** _MeiImportParser.parse (parser_support.dart:1764) jumps straight from <score> to the first <section>, then _parseMeasure (parser_support.dart:1783) only reads clef/keySig/meterSig that appear as direct children of <staff>. There is no handling of <scoreDef> or <staffDef> anywhere. The model already has lib/core/score_def.dart (ScoreDefinition with clef/keySignature/timeSignature/tempo) created specifically for this, but the parser never instantiates or consults it.
**Desired:** Per MEI 5.0 the conventional place for the initial clef/key/meter (and staff line count) is <scoreDef> and its nested <staffDef> at the start of a <section>, NOT inline in <staff>. Verovio, MuseScore and the MEI sample corpus all encode it this way. The parser should read <scoreDef>/<staffDef> @clef.shape/@clef.line, @key.sig (and child <keySig>), @meter.count/@meter.unit (and child <meterSig>), select the staffDef matching staffIndex (@n), and emit a leading Clef/KeySignature/TimeSignature into the first measure, also tracking scoreDef/staffDef changes between measures (mid-score clef/key/meter changes).
**How:** Before iterating sections, capture the score-level and section-level <scoreDef> and the <staffDef n=staffIndex+1> within it. Add a helper _meiStaffDefDefaults(scoreDef, staffDef) returning List<MusicalElement> (Clef/KeySignature/TimeSignature) reusing _meiClef/_meiKeySignature/_meiTimeSignature but reading BOTH the attribute form (@clef.shape, @clef.line, @key.sig, @meter.count, @meter.unit) and the child-element form (<clef>, <keySig>, <meterSig>). Prepend those to voice(1) of the first measure (and re-emit wherever a new <scoreDef> appears). Without this, a typical MEI import currently produces staves with no clef/key/time at all.

## 4. [HIGH/medium] (mei-import) <beam> and <tuplet> container elements drop ALL their child notes (silent data loss)
**Current:** _parseMeiLayer (parser_support.dart:1935-2046) switches only on direct children named note/rest/chord/dynam/tempo/dir/breath/caesura/repeatMark/barLine. There is no case for <beam> or <tuplet>. _meiTupletInfo (parser_support.dart:2290) does check element.name.local == 'tuplet', but it is only ever called on note/rest/chord elements, so that branch is dead — a <tuplet>...</tuplet> container is never visited and its children are never reached. Beam/tuplet are otherwise only read as note attributes (@beam, @tuplet), which is the rarer MEI encoding.
**Desired:** MEI 5.0 almost always encodes beams and tuplets as CONTAINER elements: <beam><note/><note/></beam> and <tuplet num="3" numbase="2"><note/>...</tuplet> (this is what Verovio/MuseScore export). The parser must recurse into these containers. For <beam>, emit the contained notes with BeamType.start on the first, inner on the middle, end on the last. For <tuplet>, wrap children with startTuplet(actualNotes=@num, normalNotes=@numbase)/finishTuplet().
**How:** In _parseMeiLayer add case 'beam': and case 'tuplet': that iterate child.children and dispatch each note/rest/chord through the same logic. Extract the per-element handling into a helper _appendMeiLayerChild(child, accumulator, voiceNumber, currentTimeSignature) and call it from the new container cases (recursively, since beams and tuplets nest). For <tuplet> use accumulator.startTuplet/finishTuplet with @num/@numbase; for <beam> assign BeamType across the contained notes. This is the most damaging gap after scoreDef: any beamed or tupleted passage in standard MEI currently imports as empty.

## 5. [HIGH/large] (musicxml-import) Only one part imported; multi-part/multi-staff scores collapse to a single staff
**Current:** parseMusicXmlStaff returns a single Staff for one partIndex (musicxml_parser.dart:parseMusicXML -> parser_support.dart:_MusicXmlImportParser.parse / _parsePartwise at parser_support.dart:1171). _parsePartwise throws if partIndex is out of range and only iterates parts[partIndex]. There is no parseMusicXmlScore that returns all parts, and within a part the <staff>N</staff> child of <note> is never read, so a piano part written on 2 staves (G+F) is flattened into one staff.
**Desired:** Expose a score-level import that returns every <score-part> as its own Staff (and ideally groups them into a Score), and split notes within a part onto separate staves by their <staff> element (MusicXML 4.0 <attributes><staves> + per-note <staff>). Piano, SATB, and any ensemble score is the single most common real-world MusicXML and currently loses all but one line.
**How:** Add a Score/multi-staff entry point alongside parseMusicXmlStaff. In _parseMeasure, key accumulators by (staff, voice) instead of voice only; read int staff = _childText(noteElement,'staff') and route. Read <attributes><staves> to size the staff count. Reuse the existing _VoiceAccumulator per (staff,voice). Keep parseMusicXmlStaff(partIndex,staffIndex) as the single-staff convenience wrapper.

## 6. [HIGH/large] (mei-import) Measure-level control events (<slur>, <tie>, <dynam>, <hairpin>, <fermata>, <dir>, <tempo>) with @startid/@endid are ignored
**Current:** _parseMeasure (parser_support.dart:1783) only iterates children of <staff>. Slur/tie are read solely as note attributes via _meiNote (@slur at parser_support.dart:2170, @tie at 2169). MEI control events that are siblings of <staff> inside <measure> — e.g. <slur startid="#n1" endid="#n5"/>, <tie startid endid/>, <dynam startid="#n1">p</dynam>, <hairpin form="cres" startid endid/>, <fermata startid/>, <dir startid/> — are never examined, and nothing resolves @startid/@endid/@plist against note xml:id.
**Desired:** Per MEI 5.0, slurs/ties/dynamics/hairpins/fermatas/directives are typically encoded as standalone control events at <measure> level referencing notes by @startid/@endid (a #-prefixed reference to a note xml:id), not as attributes on <note>. The parser should collect these, index xml:id -> imported Note/Chord, and attach SlurType.start/end, TieType.start/end, Dynamic, Ornament(fermata), MusicText/RepeatMark, and hairpin Dynamics to the referenced events.
**How:** Two-pass approach: (1) while building notes, record map<String xmlId, Note/Chord> (the model already exposes MusicalElement.xmlId). (2) After parsing <staff>, iterate the measure's other children (slur/tie/dynam/hairpin/fermata/dir) and resolve @startid/@endid (strip leading '#'). Set slur/tie type on the start and end notes; insert Dynamic/MusicText before the start note's slot; map <hairpin @form=cres/dim> to Dynamic(isHairpin:true). Fall back to @tstamp ordering when @startid is absent. This is the single biggest reason real MEI slurs/ties/dynamics import as bare notes.

## 7. [MEDIUM/small] (musicxml-import) <transpose> ignored: transposing-instrument parts import at written (wrong) pitch
**Current:** _parseMusicXmlAttributes (parser_support.dart:1337) handles only clef, key, and time. The <transpose> child (<chromatic>, <diatonic>, <octave-change>) is never read, and there is no transposition field applied to imported pitches. _musicXmlPitch (parser_support.dart:1531) takes step/alter/octave verbatim.
**Desired:** Either record the part transposition on the Staff so sounding pitch can be derived, or (commonly desired) apply it to produce concert pitch on import. A B-flat clarinet part currently imports a written C as sounding C instead of B-flat, which is wrong for playback/MIDI.
**How:** Parse <transpose> in attributes into a {chromatic, diatonic, octaveChange} record sticky across the part. Provide an option to apply it to each Pitch (shift by chromatic semitones + octaveChange octaves, respelling via diatonic) for concert-pitch import, defaulting to written pitch to preserve current behavior. MIDI mapping in lib/src/midi should consume the transposition.

## 8. [MEDIUM/small] (musicxml-export) Lyrics (syllables) are never exported despite being imported
**Current:** `_buildNoteXml` (lib/src/parsers/musicxml_parser.dart:242-301) never references `note.syllables`. The importer now parses `<lyric>` into `Note.syllables` (parser_support.dart:1303, `_parseMusicXmlLyrics`) and PROGRESS.md:250 explicitly lists 'round-trip de letras no export MusicXML' as outstanding. So lyrics import correctly but are dropped on export — round-trip loses all text.
**Desired:** For each `Syllable` in `note.syllables`, emit `<lyric number="i+1"><syllabic>single|begin|middle|end</syllabic><text>..</text></lyric>`, mapping `SyllableType` (lib/core/text.dart:36-47: single/initial/middle/terminal) to the MusicXML `syllabic` values.
**How:** Add a loop after the notations block in `_buildNoteXml`. Map initial->begin, middle->middle, terminal->end, single->single. Emit `<extend/>` for melisma/hyphen continuation if represented. Skip for chord tones (only the principal note carries lyrics).

## 9. [MEDIUM/small] (musicxml-export) Beams are never exported (auto-beam info lost on round-trip)
**Current:** `Note.beam` (lib/core/note.dart:38, BeamType) is parsed by the importer via `_musicXmlBeamType` (parser_support.dart:1296) but `_buildNoteXml` never emits a `<beam>` element. Consumers re-deriving beams from `<type>` may differ from the original beaming, and explicit beam groups are lost.
**Desired:** Emit `<beam number="1">begin|continue|end</beam>` from `Note.beam`, mapping BeamType.start->begin, BeamType.inner->continue, BeamType.end->end (inverse of the import aliases at parser_support.dart:386-388).
**How:** Add to `_buildNoteXml` after `<dot>`. A minimal version handles primary beam level 1; full fidelity for 16th+ would need secondary beam numbers, but level-1 alone restores most common cases.

## 10. [MEDIUM/small] (musicxml-export) Clef line mapping is wrong for treble/bass and missing octave clefs
**Current:** In `_buildMeasureXml` (lines 177-185) the `line` switch returns `'2'` as default for treble and `'4'` for bass, but it never sets the clef `<clef-octave-change>` and mishandles cases: e.g. `ClefType.bass` is explicitly mapped to line 4 (correct) yet `treble8vb`/`bass8va` octave variants (which `actualClefType` may resolve) lose their octave displacement. Also the `sign` derivation via `type.name.startsWith('treble')` (line 172) will classify any unexpected clef as 'C' on line 2, silently corrupting alto/percussion/TAB clefs.
**Desired:** Emit `<clef-octave-change>` for 8va/8vb/15ma variants and handle percussion ('percussion' sign) and TAB ('TAB' sign) clefs explicitly, rather than defaulting unknowns to a C-clef on line 2.
**How:** Replace the name.startsWith heuristic with an explicit ClefType->{sign,line,octaveChange} table. Emit `<clef-octave-change>-1</clef-octave-change>` for 8vb, `+1` for 8va. This is lower-frequency than tuplets/voices but a clear correctness defect.

## 11. [MEDIUM/small] (musicxml-export) Ties only emit start/stop, never tie-stop+tie-start for continuation; and <tied> notation missing
**Current:** `_buildNoteXml` (lines 264-272) emits a single `<tie>` whose type is 'stop' only when `note.tie == TieType.end`, else 'start'. A `TieType.continue`/middle note (stop AND start) gets only one tie, breaking chains of three+ tied notes. Also no `<tied>` element is emitted inside `<notations>`, which renderers (not just players) rely on for the visual slur of the tie.
**Desired:** For a continuation tie emit BOTH `<tie type="stop"/>` and `<tie type="start"/>`, and mirror them as `<tied>` elements inside `<notations>` per the spec.
**How:** Check for a 'continue'/middle TieType and emit two `<tie>` plus two `<tied>` elements. Add `<tied>` to the existing notations block. Low frequency but needed for correct multi-note ties.

## 12. [MEDIUM/small] (mei-import) Grace-note kind (<graceGrp>, @grace acc/unacc) is collapsed to a generic grace flag
**Current:** _meiNote (parser_support.dart:2173) sets isGraceNote via noteElement.getAttribute('grace') != null — it never reads the @grace VALUE (acc/unacc/unknown) nor @stem.mod (slash). Grace notes wrapped in a <graceGrp>...</graceGrp> container are not reached at all, because _parseMeiLayer has no case 'graceGrp' (the same container-skipping problem as <beam>/<tuplet>).
**Desired:** MEI 5.0 distinguishes @grace="acc" (acciaccatura, slashed) from @grace="unacc" (appoggiatura) and groups consecutive grace notes in <graceGrp>. The model already carries this distinction (Ornament acciaccatura/appoggiatura plus Note.isGraceNote). Acciaccatura vs appoggiatura affects both rendering (slash) and MIDI timing, so the kind should be preserved.
**How:** Read noteElement.getAttribute('grace'): 'acc'/'unacc' -> map to the existing acciaccatura/appoggiatura representation. Add case 'graceGrp' in _parseMeiLayer that recurses into its children marking each as grace (reuse the _appendMeiLayerChild helper introduced for the beam/tuplet fix). Without graceGrp handling, grace flourishes encoded as groups import as nothing.

## 13. [MEDIUM/small] (mei-export) MEI note/pitch attributes (@pname/@oct/@accid) have no exporter, though import conventions are fully defined
**Current:** The MEI importer `_meiNote` (parser_support.dart:2144) reads `@pname`, `@oct`, `@accid`/`@accid.ges`, `@dur`, `@dots`, `@beam`, `@artic`, `@tie`, `@slur`, `@ornam`, `@grace`. Nothing writes them. The model exposes everything needed: `Pitch.step/octave/accidentalType` (pitch.dart), `accidentalToAlter`/`accidentalToGlyph` maps, and `DurationType.meiDurValue`. The only MEI-ish writer in the repo, the MusicXML exporter's `_buildNoteXml` (musicxml_parser.dart:242), emits MusicXML `<pitch><step/><alter/><octave/>` — not MEI attributes.
**Desired:** A `_meiNote` writer emitting `<note pname="c" oct="4" dur="4" dots="1" accid="s"/>` with `@accid` derived from `Pitch.accidentalType` (s/f/n/x/ff/ss/...), `@artic` as a space-separated token list, `@tie`/`@slur` as i/m/t, `@grace` for grace notes, and `@beam` when set. Microtonal accidentals (quarterTone*, koma*, sagittal*) already in AccidentalType (pitch.dart:6) should map to MEI `@accid` values like `1qs`/`1qf`/`3qs`/`3qf` so they survive export.
**How:** Add an AccidentalType->MEI-@accid string map (the inverse of `_parseAccidentalType`). Map ClefType->(@shape,@line,@dis,@dis.place) by inverting `_meiClef` (parser_support.dart:2049): treble->shape=G line=2, bass->F line=4, alto->C line=3, etc.; octave clefs add dis=8/15 + dis.place. KeySignature->`@sig` by inverting `_meiKeySignature` (parser_support.dart:2115): `0`, `2s`, `3f`. TimeSignature->`@count`/`@unit`. Keep `Pitch.step` lowercase per MEI convention.

## 14. [MEDIUM/small] (midi-playback) TempoMark.beatUnit is ignored, so any non-quarter metronome mark plays at the wrong speed
**Current:** _consumeElement (midi_mapper.dart:328-338) emits MidiEvent.tempo(tick, bpm: element.bpm) using the raw bpm and never reads element.beatUnit (TempoMark.beatUnit, tempo.dart:9). MIDI tempo is always quarter-note based, so a notated 'half note = 60' is emitted as quarter=60 and plays at half the intended speed; 'dotted-quarter = 80' (compound meter) is similarly wrong.
**Desired:** Convert the notated beat unit to a quarter-note BPM before emitting: quarterBpm = bpm * (beatUnitInQuarters). E.g. half=60 -> quarter=120; dotted-quarter=80 -> quarter=120.
**How:** Map DurationType (+dot) to a quarter-note factor (half=2, dotted-half=3, quarter=1, dotted-quarter=1.5, eighth=0.5...) and multiply bpm by it when building MidiEvent.tempo. Apply the same conversion to the conductor's default tempo path (fromScore line 90, fromStaff line 148) and to MetronomeMark if it is ever mapped. Round to int for the SMF tempo meta.

## 15. [MEDIUM/small] (midi-playback) Fermata has no effect on playback duration
**Current:** Fermata is modelled as OrnamentType.fermata (ornament.dart:42) and imported (parser_support.dart:609 maps 'fermata'->OrnamentType.fermata), but _emitNote never inspects ornaments, so a note/rest under a fermata plays at its plain notated length. There is no hold at the final chord or at a caesura.
**Desired:** A note or rest carrying a fermata should be lengthened (commonly ~1.5x–2x its notated value, configurable). This is the single most-expected expressive timing cue, especially for the final note of a piece or phrase ending.
**How:** In _emitNote/_emitChord and the Rest branch, check note.ornaments for fermata/fermataBelow and multiply the SOUNDED duration AND the cursor advance by options.fermataScale (default ~1.75). Unlike articulations, a fermata DOES change the rhythmic advance (it extends real time), so this must adjust the returned tick count too — which then also shifts the metronome/other staves, acceptable since a fermata is a global hold. Consider only honouring it on single-voice measures to avoid per-voice desync.

## 16. [MEDIUM/medium] (musicxml-import) <divisions> ignored: durations come only from <type>, so type-less notes default to quarter
**Current:** _musicXmlDurationFromNote (parser_support.dart:1554) builds Duration solely from <type> and counts <dot> children: _parseDurationType(_childText(noteElement,'type')) ?? DurationType.quarter. The <duration> element and the measure's <attributes><divisions> are never read. Files that omit <type> (valid MusicXML, e.g. many exporters for rests or whole-measure content) silently become quarter notes, and there is no tick/onset value at all for timing.
**Desired:** Read <divisions> per measure (it is sticky across measures per MusicXML 4.0) and compute each note's real duration in divisions from <duration>; use it both to derive/cross-check the notated DurationType and to drive the backup/forward cursor and MIDI onset. When <type> is present prefer it for notation but still capture <duration> for timing.
**How:** Carry currentDivisions in _parseMeasure (default 1, updated from <attributes><divisions>). Map duration/divisions to a DurationType when <type> is absent (quarter = divisions, half = 2*divisions, etc.; handle dots via 1.5x). Store the raw division count for cursor math from the backup/forward fix.

## 17. [MEDIUM/medium] (musicxml-import) Slur/tie <number> (id) ignored, so overlapping slurs and multi-tie chords collapse
**Current:** _musicXmlSlurType (parser_support.dart:1626) iterates all <slur> under <notations> but stores only a single SlurType on the note (last one wins), discarding @number. _musicXmlTieType (parser_support.dart:1617) likewise reduces all <tie> to one TieType. Note carries a single slur and single tie field (lib/core/note.dart:40-43). Overlapping/nested slurs (number=1 and number=2 simultaneously) and chords where individual notes tie independently cannot be represented.
**Desired:** Preserve slur/tie identity (@number) so concurrent spanners survive, and attach ties at the chord-tone level for chords. At minimum, do not silently lose a second concurrent slur start/stop on the same note.
**How:** This needs a model change (list of slurs/ties with ids, or per-chord-note tie). If model change is too large now, at least detect and keep a start when both a start and stop occur on one note. For chords, _meiChord/chord merge path should carry per-note tie from <tied>.

## 18. [MEDIUM/medium] (musicxml-export) Multi-voice measures export empty (voices ignored)
**Current:** `_buildMeasureXml` only iterates `measure.elements` (lib/src/parsers/musicxml_parser.dart:155,209). Polyphonic measures are imported as `MultiVoiceMeasure`, which stores its notes in `_voicesByNumber`/`voices` (lib/core/voice.dart:179-187) and NOT in `elements`. The MusicXML importer creates these (parser_support.dart:1251-1266). On export a MultiVoiceMeasure yields only its leading system elements (clef/key/time) and zero notes for every voice.
**Desired:** For a `MultiVoiceMeasure`, emit each voice's notes with a `<voice>N</voice>` child, and insert a `<backup><duration>...</duration></backup>` between voices so they align to the same measure start, per the MusicXML partwise model.
**How:** Detect `measure is MultiVoiceMeasure`; iterate `sortedVoices`, emit voice 1's notes, then a `<backup>` of the measure's total divisions, then voice 2, etc. Also emit `<voice>` from `Note.voice` (lib/core/note.dart:56) which is currently never written even in single-voice output.

## 19. [MEDIUM/medium] (musicxml-export) Ornaments and grace notes dropped on export
**Current:** `_buildNoteXml` emits `<notations>` only for slur and articulations (lines 273-298); it never inspects `note.ornaments` (lib/core/note.dart:46) or `note.isGraceNote` (line 66). The importer fills both (`_musicXmlOrnaments` at parser_support.dart:1300; grace at 1282/1302). So trills, mordents, turns, and all grace notes are silently lost on round-trip.
**Desired:** Emit `<notations><ornaments><trill-mark/>|<mordent/>|<turn/>...</ornaments></notations>` mapped from `OrnamentType` (lib/core/ornament.dart:7+), and emit a `<grace/>` element at the start of a grace note (before `<pitch>`), omitting `<duration>` for grace notes as the spec requires.
**How:** Add an `_ornamentToString` switch (trill->trill-mark, mordent->mordent, turn->turn, turnInverted->inverted-turn, etc.). In `_buildNoteXml`, if `note.isGraceNote` emit `<grace/>` and skip `<duration>`. Fold ornaments into the existing `<notations>` block alongside articulations.

## 20. [MEDIUM/medium] (musicxml-export) Barlines and repeats (RepeatMark, barline/repeat barlines) not exported
**Current:** The export element loop (lines 209-237) handles no barline/repeat. `RepeatMark` (created on import at parser_support.dart:1383) falls through to nothing. There is no emission of `<barline><repeat direction="forward|backward"/></barline>` or `<barline><bar-style>light-heavy</bar-style></barline>`. The model has `BarlineType`/`RepeatType` (parser_support.dart:468-536) but the exporter writes single regular barlines only (implicit). Repeat structure and final/double barlines are lost.
**Desired:** Emit `<barline location="left|right">` with `<bar-style>` and `<repeat direction=...>`/`<ending>` based on the measure's barline/repeat elements, so repeats and section endings survive round-trip.
**How:** Detect repeat/barline elements in the measure and emit the appropriate `<barline>` at the correct location (forward repeats at left, backward/final at right). Map RepeatType.start->forward, end->backward; BarlineType.final_->light-heavy, double->light-light.

## 21. [MEDIUM/medium] (mei-export) No MEI serializer exists at all — MEI is import-only despite a "100% MEI v5" claim
**Current:** There is zero MEI write path. `MEIParser` (lib/src/parsers/mei_parser.dart) only exposes `parseMEI`, an 8-line wrapper delegating to `parseMeiStaff` (lib/src/parsers/parser_support.dart:73). A repo-wide grep for `toMei`/`MeiExport`/`writeMei`/`serializeMei`/`staffToMEI` returns 0 hits. By contrast the MusicXML side is symmetric: `MusicXMLParser.staffToMusicXML` (lib/src/parsers/musicxml_parser.dart:14) and `mergeStaffs` (musicxml_parser.dart:100) emit `<score-partwise>` v4.0. CHANGELOG.md:88 advertises 'MEI v5 100% conformance' and CHANGELOG.md:98 lists `DurationType.meiDurValue`/`fromMeiValue` 'for MEI serialization', yet those helpers are never called anywhere — there is no consumer of the write path. PROGRESS.md H1 already flags the 100% claim as an overclaim for import (~30%) and rendering (CMN-only) but does not even mention export, because none exists.
**Desired:** A public `MEIParser.staffToMEI(Staff)` (and `scoreToMEI(Score)` for multi-staff) that produces a well-formed MEI 5 document: `<mei><meiHead/><music><body><mdiv><score><scoreDef/><section>` then per-measure `<measure n><staff n><layer n>` with `<note>/<rest>/<chord>` children. This makes the advertised round-trip real and lets the existing MEI importer re-read its own output.
**How:** Mirror staffToMusicXML's XmlBuilder structure. Reuse helpers that already exist and are currently dead: `DurationType.meiDurValue` (duration.dart:77) for `@dur`, `note.duration.dots` for `@dots`, `pitch.step.toLowerCase()` for `@pname`, `pitch.octave` for `@oct`. Emit one `<staff>` per Staff and group consecutive elements of the same `Note.voice` into `<layer n=voice>` (default voice 1) — the importer reads `<layer @n>` at parser_support.dart:1940. Emit `MusicalElement.xmlId` as `xml:id` when present. Root namespace: `xmlns="http://www.music-encoding.org/ns/mei"` `meiversion="5.0"`. Add a round-trip test (export Staff -> parseMeiStaff -> compare) alongside test/parsers/lyrics_import_test.dart.

## 22. [MEDIUM/medium] (mei-export) Rich MeiHeader / FRBR model has no <meiHead> serializer
**Current:** `MeiHeader` (lib/core/mei_header.dart) models the full MEI 5 header: `FileDescription` (title/subtitle/contributors/publication/sources), `EncodingDescription` (meiVersion default '5', applications), `WorkList`/`WorkInfo`, `ManifestationList`, `RevisionDescription` — i.e. all four FRBR levels. `Score.meiHeader` (score.dart:68) holds it. CHANGELOG.md:92 advertises 'MeiHeader with full FRBR model'. No code serializes any of it; there is no `<meiHead>` writer and the importer also never builds a MeiHeader (it returns a single Staff).
**Desired:** A `<meiHead>` serializer producing the required `<fileDesc>` (with `<titleStmt><title>` and `<respStmt><persName role=...>` from contributors, `<pubStmt>`), plus optional `<encodingDesc>`, `<workList>`, `<manifestationList>`, `<revisionDesc>`. When `Score.meiHeader` is null, emit a minimal valid `<meiHead><fileDesc><titleStmt><title/></titleStmt><pubStmt/></fileDesc></meiHead>` from `Score.title`/`composer` so output is schema-valid.
**How:** Map `ResponsibilityRole` (mei_header.dart:8) enum to MEI `@role` strings (composer/arranger/editor/lyricist/...). Emit `EncodingDescription.applications` as `<appInfo><application><name/></application></appInfo>`. `<meiHead>` is required by the MEI schema and `<fileDesc>` is its only required child, so this is needed for any output to validate against the MEI 5 RNG.

## 23. [MEDIUM/medium] (mei-export) No multi-staff / Score-level MEI structure (mdiv/scoreDef/staffGrp)
**Current:** The data model has full multi-staff structure — `Score.staffGroups` of `StaffGroup` (with `BracketType` brace/bracket), `ScoreDefinition` (score_def.dart: global clef/key/time/tempo/defaultStaffLines), and `Staff.lineCount` (staff.dart:32, MEI `@lines`). The MusicXML side already handles multi-staff via `mergeStaffs` (musicxml_parser.dart:100). But there is no MEI exporter to consume any of this, so a Score with two staves cannot be written to MEI at all.
**Desired:** A `scoreToMEI(Score)` emitting `<mdiv><score>` with a `<scoreDef>` carrying `<staffGrp>` and one `<staffDef n=i lines=.. clef.shape/clef.line key.sig meter.count/meter.unit>` per staff, then a `<section>` whose `<measure>` elements contain one `<staff n=i>` per staff. StaffGroup bracket -> `<staffGrp @symbol="brace|bracket">`.
**How:** Pull global clef/key/time from `Score.scoreDefinition` (score_def.dart:28) into `<scoreDef>`/`<staffDef>` attributes (`@clef.shape`, `@clef.line`, `@key.sig`, `@meter.count`, `@meter.unit`, `@lines` from Staff.lineCount). Iterate measures index-aligned across `Score.allStaves` (score.dart:88) so each `<measure>` gets a `<staff n>` per part, matching how the importer reads multiple `<staff>` elements per measure (parser_support.dart:1802).

## 24. [MEDIUM/medium] (midi-playback) Crescendo/diminuendo (hairpins) produce no velocity ramp — and actually RESET velocity to 96
**Current:** velocityFromDynamic (midi_models.dart:245) has no case for DynamicType.crescendo / DynamicType.diminuendo / DynamicType.subito etc., so they fall through to the default '_ => 96'. In _consumeElement (midi_mapper.dart:351) ANY Dynamic element sets _voiceVelocity[voice] = velocityFromDynamic(type); so encountering a 'crescendo' marker silently snaps the running velocity to a flat 96 with no gradual change. Dynamic.isHairpin / Dynamic.length (dynamic.dart) are never consulted.
**Desired:** A crescendo/diminuendo should ramp velocity from the current dynamic to the target dynamic over the span of notes it covers (interpolate per note, or emit CC11 expression ramps). At minimum, a hairpin marker must NOT clobber the current velocity with 96.
**How:** Detect Dynamic where type is crescendo/diminuendo (or isHairpin==true) and treat it as a span, not an instantaneous level: record a pending ramp with a start velocity (current _voiceVelocity) and resolve the end velocity when the next concrete dynamic (or hairpin stop) is seen, then distribute interpolated velocities across the intervening notes. Short term, in velocityFromDynamic return the current velocity (caller-supplied) rather than 96 for crescendo/diminuendo so they become no-ops instead of resets. Also map the missing literal levels (pianississimo→~26, fffff/ffffff→~127, subito/possibile/menoMosso/piuMosso → keep current) to avoid the 96 fallback.

## 25. [MEDIUM/medium] (midi-playback) Grace notes ADD time instead of stealing it — they push the main note late and overflow the measure
**Current:** _emitNote (midi_mapper.dart:385-409) for a grace note returns _durationToTicks(... isGraceNote:true) which is realValue*4*graceDurationScale ticks (graceDurationScale default 0.25). Because _consumeElement adds that return value to localTick (midi_mapper.dart:236-242 / 213-221), the grace note consumes real time and shifts every following note later. A measure with grace notes then trips the 'Measure overflowed by N ticks' warning and desyncs against other staves/metronome.
**Desired:** Grace notes must not change the total measure length. An acciaccatura should steal a small slice from the FOLLOWING principal note (play just before the beat, principal starts slightly later but ends on time); an appoggiatura should steal from the principal (split its duration). Either way the sum of (grace + principal) must equal the principal's notated duration so downstream timing is unaffected.
**How:** Handle grace notes as a look-ahead: when a grace (or run of graces) precedes a principal Note/Chord, do not advance the cursor for the grace; instead place the grace noteOn at principalStart (acciaccatura) or principalStart, then start the principal at principalStart+graceTicks and subtract graceTicks from the principal's sounded duration. The grace's own return value must be 0 so the cursor only advances by the principal's full notated value. Distinguish acciaccatura vs appoggiatura via the note's ornament/alternatePitch or a model flag.

## 26. [MEDIUM/medium] (model-coverage) Figured bass (basso continuo) is model-only: no parse, render, or play path
**Current:** lib/core/figured_bass.dart defines FiguredBass + FigureElement (mapped to MEI <fb>/<f>, with FigureAccidental and FigureSuffix), exported via core.dart. But FiguredBass() is instantiated NOWHERE outside its own file (grep for `FiguredBass(` in lib hits only figured_bass.dart). The MusicXML measure-content switch (parser_support.dart ~line 1223: attributes/direction/barline/note/backup/forward) has no <figured-bass> case; the MEI layer switch (parser_support.dart:1944-2044) and measure switch (~1823) have no <fb> case. No renderer references it (grep across lib/src/rendering = 0 hits). MidiMapper element dispatch (midi_mapper.dart:286-313, 901-913) only handles Note/Chord/Rest/Tuplet. So figured bass cannot be imported, drawn, or played.
**Desired:** Import figured bass from MusicXML/MEI and render stacked figures beneath the bass note.
**How:** Smallest high-value slice: (1) add a `figuredBass` field on Note (or a measure-level element) and parse MusicXML <figured-bass><figure><figure-number>/<prefix>/<suffix>/<extend> and MEI <harm><fb><f> into FigureElement; (2) add a FiguredBassRenderer that stacks FigureElement.numeral with SMuFL figured-bass digit glyphs (figbass0..figbass9) + accidental glyphs, vertically below the bass note's X, using staffSpace units, mirroring the existing dynamics/lyrics floating-element placement. Render before attempting playback realization.

## 27. [MEDIUM/medium] (model-coverage) Harmony / chord symbols absent as a CMN concept; only MEI analytic <harm> labels modeled and never used
**Current:** There is NO chord-symbol / lead-sheet harmony model class in core (grep `ChordSymbol|class Harmony|root-step|kind` in lib finds nothing relevant). The only harmony type is lib/core/harmonic_analysis.dart HarmonicLabel (MEI <harm>: symbol, scaleDegree, melodicFunction, ChordTable/ChordDefinition for analytic markup). HarmonicLabel() is instantiated only in harmonic_analysis.dart. MusicXML <harmony> (the common pop/jazz chord symbol with root-step/kind/bass) is silently dropped: the measure switch in parser_support.dart (~1223) has no `harmony` case, and musicxml_parser.dart only reads it for extractMetadata. No renderer draws chord symbols; MidiMapper ignores them.
**Desired:** Parse and render chord symbols above the staff from MusicXML <harmony>.
**How:** Highest-value common case is the MusicXML <harmony> chord symbol (lead sheets/pop), NOT the analytic <harm>. Add a ChordSymbol element (root, kind->suffix string, optional bass) ; add a `harmony` case in the measure-content switch that emits it at the current measure position; render as a floating text element above the staff using the existing symbol_and_text_renderer text path (same layer that draws dynamics words). Map HarmonicLabel.symbol the same way so the existing MEI model also renders. This reuses the floating-text placement already built for dynamics.

## 28. [MEDIUM/large] (musicxml-import) <backup>/<forward> are no-ops, so multi-voice and cross-staff timing is wrong
**Current:** In _MusicXmlImportParser._parseMeasure the switch handles 'backup' and 'forward' with an empty body (parser_support.dart:1245-1247). Voices are accumulated purely by append order keyed on <voice>. MusicXML places voice 2 / staff 2 content after a <backup> that rewinds the cursor; because backup/forward do not move any cursor, every voice is treated as starting at beat 0 only by virtue of being a separate accumulator, and any rest-less alignment, mid-measure voice switches, or cross-staff chords are mis-timed. Notes with <chord/> that follow a backup also cannot merge correctly.
**Desired:** Track a running tick cursor per measure: <forward> advances it, <backup> rewinds it, each note advances it by its (divisions-based) duration. Use the cursor to position voice/staff content so voices align temporally, matching MusicXML 4.0 semantics. This is also a prerequisite for correct multi-voice rendering.
**How:** Introduce a divisions-aware duration in ticks (see divisions gap). Maintain int cursorTicks; on note (non-chord) add duration ticks, on <chord/> do not advance, on <backup><duration> subtract, on <forward><duration> add. Store onset per element so layout/MIDI can use it. The voice accumulators already exist; add an onset field to the appended elements or to a parallel structure.

## 29. [MEDIUM/large] (midi-playback) Ornaments (trill, mordent, turn, tremolo) are never expanded — they play as a single plain note
**Current:** _consumeElement / _emitNote in midi_mapper.dart never inspect note.ornaments (Note.ornaments, lib/core/note.dart:45) or note.tremoloStrokes (note.dart:59). Ornaments incl. trill/mordent/invertedMordent/turn are imported (_parseOrnamentType, parser_support.dart:596). A trilled half note is rendered with a trill mark but sounds as one sustained pitch; tremolo strokes are silent rhythmically.
**Desired:** Expand ornaments into the actual note stream at playback time: trill = rapid alternation between the principal and the upper diatonic neighbour for the note's duration; mordent = principal-lower-principal; invertedMordent/shortTrill = principal-upper-principal; turn = upper-principal-lower-principal; tremolo (n strokes) = principal repeated at the subdivision implied by stroke count. Honour the key signature / active accidentals for the auxiliary pitch, and trillSharp/trillFlat/trillNatural for the alteration.
**How:** Add an _expandOrnament(note, durationTicks) that returns a list of (midiNote,startOffset,durTicks) sub-events instead of one. Needs the active KeySignature (and the within-measure accidental state the renderer already computes) to pick the correct neighbour semitone — thread a small key/accidental context into _TrackEventBuilder. Make expansion opt-in via a MidiGenerationOptions.expandOrnaments flag so users who want literal note durations keep them. For tremolo, subdivide durationTicks by 2^strokes (single-note tremolo) or alternate between the two chord notes (two-note tremolo).

## 30. [MEDIUM/large] (model-coverage) Tablature is model-only: rich tab model + Note.tabFret/tabString fields, but no parse, render, or tab-staff layout
**Current:** Two parallel tab models exist: lib/core/tablature.dart (TabNote/TabGrp/TabString/TabTuning/TabDurSym, MEI-aligned) and lib/src/music_model/tablature.dart (a second TabNote/TabChord/TabClef/techniques, much of it Placeholder — TabNote.getPitch returns `Pitch(step:'C',octave:4) // Placeholder`, TabUtils.* return empty/`true`). Note already has tabFret/tabString/isTabNote (note.dart:71-83) but _meiNote (parser_support.dart:2144-2176) never reads @tab.fret/@tab.string, so even tab notes import as ordinary pitched notes. No renderer handles fret numbers or a TAB staff (grep tabFret/TabClef/6stringTabClef in lib/src/rendering = 0). LayoutEngine dispatch (layout_engine.dart:286-313) has no TabNote/TabGrp branch. MidiMapper has no tab handling.
**Desired:** Import tab notes and render a real TAB staff with fret numbers.
**How:** Phase it: (1) parse @tab.fret/@tab.string in _meiNote into the existing Note fields (1-line change of plumbing); (2) implement TabString->Pitch + fret offset so a TabNote resolves to a real Pitch (the current getPitch is a Placeholder) — needed for both render labels and MIDI; (3) add a TabStaffRenderer that, when Staff is a tab staff, draws fret digits on string lines instead of noteheads. Consolidate the duplicate tablature.dart models first to avoid divergence. This is the largest of the gaps because it needs a distinct staff layout.

## 31. [MEDIUM/large] (model-coverage) Mensural notation is model-only: classes exist but no parse, render, or play path
**Current:** lib/core/mensural.dart defines MensuralNote, MensuralRest, Ligature, Mensur, ProportMark, plus mensuralToModernDuration()/mensuralDurationToValue() converters. None are instantiated outside mensural.dart (grep `MensuralNote(|Ligature(|Mensur(` in lib = only the def file). No MEI mensural parsing (parser_support.dart layer/measure switches have no mensur/ligature/proport handling; _meiNote reads only CMN attrs). No renderer (grep MensuralNote in lib/src/rendering = 0) — no oblique/diamond noteheads, no ligature glyphs, no mensuration signs. LayoutEngine and MidiMapper never branch on mensural types. PROGRESS.md H1 already flags mensural render as the biggest, highest-risk effort.
**Desired:** Import mensural MEI and transcribe to CMN for view/play via the existing converter.
**How:** Authentic mensural engraving (precomposed ligatures, mensuration signs, coloration) is a whole notation system and out of scope for one step. The pragmatic high-value step is a transcription path: parse mensural MEI into MensuralNote/Ligature, then map each MensuralNote via the already-present mensuralToModernDuration() to ordinary Note/Chord so the existing CMN renderer + MidiMapper handle it. This unlocks import+view+play without new glyph work; authentic glyphs can follow the Gregorian precedent (dedicated font/renderer).

## 32. [LOW/small] (musicxml-import) Lyric <extend> (melisma) and <elision> are dropped on import
**Current:** _parseMusicXmlLyrics (parser_support.dart:1496) reads only <text> and <syllabic> per <lyric>. <extend> (melisma word-extension line) and <elision> (multiple syllables under one note joined by an elision slur) are ignored. The Syllable model has no extension flag (lib/core/text.dart:56) though the renderer/Neume path elsewhere supports melisma lines and the chant code added hyphenAfter.
**Desired:** Capture <extend> so vowel-extension lines can render under melismas, and handle <elision> by concatenating the multiple <text> runs (e.g. with an elision character) into the syllable. Multi-verse import already works; this completes common lyric encoding.
**How:** Add a hasExtension (and optionally elision) field to Syllable, set it when lyric.findElements('extend') is non-empty. For elision, iterate the interleaved <syllabic>/<text>/<elision> sequence within one <lyric> and join the text runs. The CMN renderer would then draw the extension line similar to the existing chant melisma pass.

## 33. [LOW/small] (musicxml-import) Grace-note slash (acciaccatura vs appoggiatura) and steal-time not captured
**Current:** _parseMusicXmlNoteNode sets isGraceNote from the presence of <grace> (parser_support.dart:1282) but never reads the <grace> element's slash / steal-time-following / steal-time-previous attributes. The grace note also goes through _musicXmlDurationFromNote and defaults to a quarter when <type> is missing. OrnamentType has acciaccatura/appoggiatura values (lib/core/ornament.dart) but the import cannot distinguish them.
**Desired:** Read <grace slash="yes"> to mark acciaccatura (slashed) vs appoggiatura, and prefer DurationType.eighth as the grace default (matching the JSON parser's grace handling at parser_support.dart:643-662). This affects rendering of the slash and MIDI timing of grace notes.
**How:** In _parseMusicXmlNoteNode read the <grace> element attributes; pass grace:true into a grace-aware duration helper so the default is eighth not quarter; set a slash flag (needs a field on Note or mapping to an ornament type).

## 34. [LOW/small] (musicxml-import) <technical> notations not imported in MusicXML path despite model support
**Current:** The MusicXML note path reads <articulations> and <ornaments> and <fermata> under <notations> (parser_support.dart:1561-1615) but never reads <technical> (fingering, string, fret, harmonic, up-bow/down-bow live under articulations here but pluck/snap-pizzicato/stopped/open-string/bend/tap/etc. live under technical in MusicXML). Note.techniques exists and PlayingTechnique/TechniqueType are modeled and parsed in the JSON path (_parseTechniqueList at parser_support.dart:711), but the MusicXML importer leaves note.techniques empty.
**Desired:** Parse <notations><technical> children into Note.techniques so fingerings, harmonics, and string/fret technique markings survive import, consistent with the JSON importer.
**How:** Add a _musicXmlTechniques(noteElement) mirroring _musicXmlArticulations but reading the <technical> parent, mapping element local names to TechniqueType, and pass the result into the Note(techniques:) constructor argument which is currently omitted in _parseMusicXmlNoteNode.

## 35. [LOW/small] (mei-import) <space>, <mRest>, <mSpace>, and <multiRest> are dropped, breaking voice timing/alignment
**Current:** _parseMeiLayer (parser_support.dart:1943) has no case for <space>, <mRest>, <mSpace> or <multiRest>; they fall through the switch and are discarded. The model already defines lib/core/space.dart (Space, MeasureSpace), so the target types exist but are unused by the importer.
**Desired:** MEI 5.0 uses <space dur="4"/> for invisible time-fillers (common for aligning polyphonic voices), <mRest/> for a whole-measure rest, <mSpace/> for an invisible whole-measure, and <multiRest num="N"/> for multi-measure rests. These carry duration and are essential for correct rhythmic alignment between layers; they should map to Space(duration), a full-measure Rest, MeasureSpace(), and MeasureSpace(measureCount:N) respectively.
**How:** Add cases in _parseMeiLayer: case 'space' -> accumulator.append(Space(duration: Duration(_parseDurationType(@dur), dots:@dots))); case 'mRest' -> append a whole-measure Rest sized from currentTimeSignature; case 'mSpace' -> append MeasureSpace(); case 'multiRest' -> append MeasureSpace(measureCount:@num). Low effort, fixes silent loss of beats that currently corrupts multi-voice alignment.

## 36. [LOW/small] (midi-playback) Chord arpeggiation and roll direction are not realised in playback
**Current:** _emitChord (midi_mapper.dart:412-446) emits every chord tone with the identical startTick, so all chords are played as block chords. There is no notion of an arpeggio/roll (OrnamentType.arpeggio exists in the model, ornament.dart, and arpeggio is a standard MusicXML notation).
**Desired:** A chord marked with an arpeggio/roll should stagger its note-ons by a small offset (bottom-up for an up-arrow roll, top-down for a down roll), keeping the same note-off so total duration is unchanged.
**How:** If the Chord (or its notes' ornaments) carries arpeggio/non-arpeggio info, sort the chord notes by pitch and add an incremental startTick offset (e.g. ticksPerQuarter/32 per note, direction by roll type) in _emitChord. Gate behind a MidiGenerationOptions.arpeggioSpreadTicks (0 = disabled) to preserve current block-chord behaviour by default.

## 37. [LOW/medium] (musicxml-import) Only the first <beam> level is read; secondary beams (16th/32nd subdivisions) are lost
**Current:** _musicXmlBeamType (parser_support.dart:1637) does noteElement.findElements('beam').firstOrNull and returns a single BeamType. MusicXML encodes one <beam number="1|2|3"> per beam level; taking only the first drops the secondary/tertiary beam information, so a beamed group of sixteenths vs eighths-with-sixteenth-subgroups cannot be distinguished from the import.
**Desired:** Read all <beam> elements with their @number to capture multi-level beaming (begin/continue/end/forward hook/backward hook per level), matching MusicXML 4.0. This lets the engraver honor the source's exact beam subdivisions instead of re-deriving them.
**How:** Return a structure of per-level BeamType keyed by @number, and also support 'forward hook'/'backward hook' values. The Beam model (lib/core/beam.dart) and beam grouping currently rely on auto-beaming; feed the explicit levels through so manual beam groups from the source are respected.

## 38. [LOW/medium] (musicxml-export) <duration> hardcoded to 1 for every note and rest (rhythm is destroyed)
**Current:** Both `_buildNoteXml` (line 259) and `_buildRestXml` (line 308) emit `builder.element('duration', nest: '1')` unconditionally, and no `<divisions>` is ever written in `<attributes>` (lines 163-206). MusicXML duration is expressed in divisions-per-quarter; emitting `1` for a whole note, a sixteenth, and a dotted half alike makes the timing meaningless. The importer reads `<duration>` for timing fallback, so a parse->export->parse loses all rhythmic proportion when `<type>` is absent, and the file is wrong for any consumer that trusts `<duration>`.
**Desired:** Emit a `<divisions>` value (e.g. 480 or 768) once per part in the first measure's `<attributes>`, and compute each note/rest `<duration>` from its `DurationType` and dots (and the active tuplet ratio).
**How:** Add a `divisions` constant; map DurationType to ticks (quarter=divisions, eighth=divisions/2, etc.), multiply by dot factor (1.5, 1.75) and by tuplet `normalNotes/actualNotes`. Write `<divisions>` in the attributes block of measure 1. This must be coordinated with the backup duration in the multi-voice fix.

## 39. [LOW/medium] (midi-playback) Slurs do not produce legato; repeated equal pitches under a slur are not connected
**Current:** note.slur (SlurType, note.dart:42) is never read in lib/src/midi/*. Each non-tied note gets an unconditional noteOff at its end tick (_emitTiedNote, the 'case null' branch, midi_mapper.dart:518-540) producing a small gap between consecutive notes regardless of slur. Only ties (TieType) hold; phrasing slurs are inaudible.
**Desired:** Notes spanning a slur should be played legato — overlap or remove the inter-note gap (note-off of the previous note at/after the next note-on). At minimum, no silence between slurred notes; ideally a slight overlap so the synth does not retrigger an envelope gap.
**How:** Track a 'currently under slur' flag per voice driven by SlurType.start/end on notes. While the flag is active, defer the previous note's noteOff to coincide with the next note's noteOn (or +1 tick after) instead of at the previous note's nominal end. Be careful with the monophonic-channel case: overlapping the same pitch needs a noteOff-before-noteOn to avoid a stuck note; for different pitches a small overlap is fine.

## 40. [LOW/medium] (midi-playback) No swing / rhythmic feel option for playback
**Current:** All durations are emitted strictly literally via _durationToTicks (midi_mapper.dart:866). There is no swing handling anywhere (grep 'swing' in lib/src/midi/* returns nothing) and no option in MidiGenerationOptions (midi_models.dart:166).
**Desired:** Optional swing feel: delay the off-beat eighth (or sixteenth) of each beat by a configurable ratio (e.g. triplet/65% swing) so jazz/pop scores play back with the intended groove.
**How:** Add MidiGenerationOptions.swingRatio (1.0 = straight) and swingUnit (eighth/sixteenth). In _buildPlayedMeasureTimeline or a post-pass over emitted events, for each beat lengthen the on-beat unit and shorten the following off-beat unit so the pair still sums to the beat. Cleanest as a quantize-aware post-process on noteOn/noteOff ticks keyed to beat position derived from the active TimeSignature. Default off (straight) to avoid changing existing output.

## 41. [LOW/medium] (model-coverage) Percussion has clef + staff scaffolding but no unpitched note model, authentic render, or GM drum playback
**Current:** Percussion scaffolding exists: clef.dart ClefType.percussion/percussion2 map to SMuFL unpitchedPercussionClef1/2 and render; staff.dart lineCount supports 1-line percussion staves; staff_group.dart StaffGroup.percussion factory exists. But there is NO unpitched/percussion note model: Note (note.dart) has no notehead-shape, unpitched-display-step, or instrument field, and grep for `unpitched|percussion|NoteheadShape` in lib/core finds only the clef/staff scaffolding. A percussion staff can therefore only hold ordinary pitched round-notehead Notes. MIDI: midi_mapper.dart assigns a configured channel+programChange per staff (lines 124-145) with no channel-9 (GM drum) path and no note->drum-key mapping, so 'percussion' plays as a pitched instrument.
**Desired:** Model unpitched percussion notes and play them on GM channel 9.
**How:** Two independent slices, both medium: (render) add a NoteheadShape field on Note and an instrument-to-line map so a percussion staff draws x/diamond heads on assigned lines; (play) add an `isPercussion`/channel-9 flag on the staff's MidiInstrumentAssignment and a Note->GM-drum-note table (e.g. 38=snare, 42=closed hi-hat) so percussion staves emit on channel 9 ignoring programChange. The MIDI slice is the cheaper, higher-value half since playback currently mis-voices any drum part.
